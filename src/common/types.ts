export enum Size {
	XS,
	S,
	M,
	L,
	XL
}

export enum ButtonVariant {
	Outlined,
	Contained,
	Text
}

export enum ButtonShape {
	Pill,
	RoundedCorner,
	Sharp
}

export enum ButtonColor {
	Primary,
	Secondary,
	Warning,
	Danger,
	Default
}